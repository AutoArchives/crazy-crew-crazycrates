name: '${name}'
main: '${group}.${name}'

version: '${version}'
api-version: '${apiVersion}'
description: '${description}'

bootstrapper: 'com.badbones69.crazycrates.paper.PluginStarter'
loader: 'com.badbones69.crazycrates.paper.CrazyLoader'

dependencies:
  server:
    CMI:
      load: BEFORE
      required: false
      join-classpath: true
    DecentHolograms:
      load: BEFORE
      required: false
      join-classpath: true
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true