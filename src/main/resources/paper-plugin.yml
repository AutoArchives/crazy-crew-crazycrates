name: '${name}'
main: '${group}.CrazyCrates'

version: '${version}'
api-version: '1.20'
description: '${description}'

bootstrapper: '${group}.CrazyStarter'

dependencies:
  - name: CMI
    required: false
    bootstrap: false
  - name: DecentHolograms
    required: false
    bootstrap: false
  - name: PlaceholderAPI
    required: false
    bootstrap: false